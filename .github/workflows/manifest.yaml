name: manifest

on:
  pull_request:
    paths:
      - config/**
      - .github/workflows/manifest.yaml

jobs:
  manifest:
    uses: int128/kubebuilder-workflows/.github/workflows/manifest.yaml@10982c4f6d8207fb947133819ceddd52b2e3f8b7 # v1.4.0
    permissions:
      contents: read
      pull-requests: write
