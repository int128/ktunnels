name: manifest

on:
  pull_request:
    paths:
      - config/**
      - .github/workflows/manifest.yaml

jobs:
  manifest:
    uses: int128/kubebuilder-workflows/.github/workflows/manifest.yaml@60df68d907adafdca7abebaf7a7cda05ade74022 # v1.3.0
    permissions:
      contents: read
      pull-requests: write
